# For Heroku command
heroku pg:reset DATABASE
heroku run rails db:migrate
heroku run rails db:seed

# IDEA
Should export to file txt for storage
Server will set a schedule for summary report using FCM (Now, the report should be delivered weekly)
